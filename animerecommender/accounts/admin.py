from django.contrib import admin

# Register your models here.
#Nothing to register here, since we're using Django's built in user's models
#Users are included already when logged in to the admin page
